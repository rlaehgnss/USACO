#include <bits/stdc++.h>
using namespace std;

int t, n;
char b[2020], e[2020];

int main() {
   scanf("%d", &t);
   while(t--) {
      scanf("%d", &n);
      for (int i = 1; i <= n; i++) scanf(" %c", b[i]);
      for (int i = 1; i <= n; i++) scanf(" %c", e[i]);

   }
}
